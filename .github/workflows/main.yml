name: Deploy to server

on:
  push:
    branches:
      - master
  workflow_dispatch:

env:
  ENV_TELEGRAM_BOT_TOKEN: ${{ secrets.ENV_TELEGRAM_BOT_TOKEN }}
  ENV_OPENAI_API_KEY: ${{ secrets.ENV_OPENAI_API_KEY }}
  ENV_ALLOWED_USER_IDS: ${{ secrets.ENV_ALLOWED_USER_IDS }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/gpt-translator-bot:latest

      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        run: |
          ssh-keyscan -H ${{ secrets.SERVER_IP }} >> known_hosts
          ssh-keygen -lf known_hosts
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USERNAME }}
          port: 22
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/${{ secrets.SERVER_USERNAME }}/gpt-translator-bot
            docker-compose pull
            docker-compose up -d
